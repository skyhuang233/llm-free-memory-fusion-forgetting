B. Beta-Mixture Gating Model (BMM)
Score normalization. Given an incoming memory item, we compute matching scores against n candidate sessions, producing {s i } n i=1 . Since Beta distributions are defined on (0, 1), we apply min-max normalization: x i =    ϵ + (1 -2ϵ) s i -s min s max -s min , s max > s min , 0.5, s max = s min , x i ∈ (0, 1), (17) where s min = min i s i , s max = max i s i , and ϵ is a small constant to avoid boundary values. Two-component Beta mixture. We fit a two-component Beta mixture model to {x i }: p(x) = π 0 Beta(x; α 0 , β 0 ) + π 1 Beta(x; α 1 , β 1 ), π k ≥ 0, k∈{0,1} π k = 1, (18) with Beta(x; α, β) = Γ(α + β) Γ(α)Γ(β) x α-1 (1 -x) β-1 . ( 19 ) We interpret the two components as low-compatibility vs. high-compatibility regimes. EM fitting with log-space responsibilities. We estimate parameters via EM for a fixed number of iterations (or until convergence). Introduce latent assignments z i ∈ {0, 1} and compute responsibilities r ik = p(z i = k | x i ). Initialization (quantile-based). We initialize the two components to be separated in mean by using the empirical 30th and 70th percentiles of {x i }. Let q 0.3 and q 0.7 denote these quantiles. We initialize component means as µ 0 ← q 0.3 and µ 1 ← q 0.7 and set initial concentrations (e.g., via a small fixed variance), then obtain (α k , β k ) by moment matching. E-step. For numerical stability, we compute responsibilities in log-space: log rik = log π k + log Beta(x i ; α k , β k ), r ik = exp(log rik ) k ′ ∈{0,1} exp(log rik ′ ) . ( 20 ) Define effective counts N k = n i=1 r ik . M-step (mixture weights). π k ← N k n . (21) M-step (Beta parameters via responsibility-weighted moments). Rather than numerically optimizing the Beta loglikelihood, we adopt a lightweight update using weighted moments. Compute the responsibility-weighted mean and variance: µ k = 1 N k n i=1 r ik x i , σ 2 k = 1 N k n i=1 r ik (x i -µ k ) 2 . ( 22 ) For Beta(α, β), µ = α/(α + β) and σ 2 = µ(1-µ) κ+1 with concentration κ = α + β. Thus, κ k ← µ k (1 -µ k ) σ 2 k -1, α k ← µ k κ k , β k ← (1 -µ k )κ k . (23) In practice, we clamp σ 2 k away from zero and clip κ k , α k , β k to avoid degenerate updates. Identifying the high-compatibility component. After fitting, we designate the component with larger Beta mean as the high component: k ⋆ = arg max k∈{0,1} α k α k + β k . ( 24 ) The gate value for a candidate score x is the posterior of this high component: g(x) = p(z = k ⋆ | x) = π k ⋆ Beta(x; α k ⋆ , β k ⋆ ) k∈{0,1} π k Beta(x; α k , β k ) . ( 25 ) Algorithm 1 Beta-Mixture-Gated Memory Fusion Require: incoming item m, candidates C, threshold τ , min-keep m min , EM iters T 1: s i ← score(m, c i ) for each c i ∈ C 2: x i ← minmax to unit(s i ) 3: if max s i = min s i then 4: x i ← 0.5 for all i 5: end if 6: Initialize 2 components using quantiles (q 0.3 , q 0.7 ) of {x i } 7: for t = 1 to T do where τ is a posterior threshold. To prevent over-filtering, if fewer than m min candidates satisfy the condition, we keep the top-m min candidates ranked by x i (equivalently, by the original matching scores). Usage in memory fusion. The retained candidates define a filtered set of plausible merge targets. The incoming memory item is merged into the most compatible retained session (under the currently selected structure), or stored as a new session if no candidate is retained. This procedure replaces fixed similarity thresholds with an adaptive, distribution-aware gating rule that is robust to score distribution shifts across conversation types and interaction stages. Computational complexity. For n candidate sessions, each EM iteration runs in O(n) time. Since the number of mixture components is fixed to two and the number of EM iterations is small and bounded, the overall gating cost is negligible compared to retrieval and response generation. The memory overhead is O(n) for storing normalized scores and responsibilities.