C.2. Feature Definition Table
As summarized in Table 5, these features are intentionally simple and interpretable, focusing on structural signals rather than deep semantic representations. This design keeps structure selection lightweight and stable, and avoids introducing additional reasoning overhead into the control layer. Algorithm 2 Offline Structure Labeling via Interaction-Derived Rewards Require: Training conversations D, structures S = {LINEAR, GRAPH, HIERARCHICAL} Require: Fixed agent pipeline A(•; s) instantiated with structure s Require: Reward weights λ judge , λ mem (e.g., 0.7/0.3) Ensure: Labeled dataset T = {(x, y)} 1: T ← ∅ 2: for each conversation/session instance ξ ∈ D do 3: Extract feature vector x ← Feat(ξ) 4: for each s ∈ S do 5: Run agent with structure s: âs ← A(ξ; s) 6: Compute judge reward: r judge (s) ← Judge(â s , ξ) 7: Compute memory utilization reward: r mem (s) ← MemUtil(â s , ξ) 8: r(s) ← λ judge r judge (s) + λ mem r mem (s) 9: end for 10: y ← arg max s∈S r(s) 11: (reward-optimal structure label) 12: T ← T ∪ {(x, y)} 13: end for for each mini-batch B ⊂ T do 4: x ← Normalize(x) for all (x, y) ∈ B 5: p ← Softmax(f θ (x)) 6: θ ← θ -η∇ θ (x,y)∈B L(p, y) 7: end for 8: Early stop if validation performance does not improve 9: end for 10: return θ hybrid retrieval pipelines commonly used in LLM-based systems. Overall cost. Compared to baselines with fixed memory structures, FluxMem introduces modest additional computation for structure selection and probabilistic gating. Empirically, this overhead is minor relative to LLM decoding cost and does not significantly affect end-to-end latency, while enabling more adaptive and selective memory utilization.